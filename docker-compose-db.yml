version: "3"

services:
  mongodb:
    image: mongo:latest
    volumes:
      - mongodata:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=cash
      - MONGO_INITDB_ROOT_PASSWORD=123
      - MONGODB_DATABASE=bikes
    ports:
      - 27017:27017
volumes:
  mongodata:
